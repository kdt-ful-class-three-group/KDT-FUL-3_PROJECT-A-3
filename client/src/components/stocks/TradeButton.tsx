import { Button } from "../common/Button"

export function TradeButton() {
  
  return (
    <div>
      <Button
        name="판매"
        onClick={() => { }}
      />
      <Button
        name="구매"
        onClick={() => { }}
      />
    </div>

  )
}